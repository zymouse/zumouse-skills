帮我制作一个OTA发版工作流的skills:
- 描述: 自动驾驶系统的ros2工作空间, 自动驾驶系统发版工作，分为自动驾驶园区版本和自动驾驶公开道路版本
  - 自动驾驶园区版本工作空间位置: ~/pix/robobus/autoware-robobus.dev-master.test/
  - 自动驾驶公开道路版本工作空间位置: ~/pix/robobus/autoware-robobus.dev-master/
  - 编译指令: 园区版本和公开道路版本编译指令一样的，主机架构不一样编译指令不一样
    amd64架构: colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release --packages-skip yabloc_image_processing bevfusion nvenc_multicam
    arm64架构: colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release --packages-skip autoware_elevation_map_loader autoware_lidar_apollo_instance_segmentation yabloc_image_processing nebula_ros nebula_examples nebula_tests blind_spot_monitor cloud_control_platform_msgs database_operations_msgs get_osm_info get_osm_info_msgs hmi_launch mongodb_operations mqtt_protobuf_test pix_io_context pix_protocol_adapter pix_tcp_driver pixmoving_api_central_analysis pixmoving_api_central_business pixmoving_api_central_common pixmoving_api_central_platform pixmoving_chassis_interface pixmoving_chassis_interface_common pixmoving_hmi_msgs pixmoving_hmi_stack pixmoving_ros_mqtt_bridge pixmoving_ros_proto_bridge pixmoving_ros_zenoh_bridge pixmoving_vehicle_display_bridge relay_autoware_transfer circular_publisher crow_vendor failcase_tools failcase_ui sqlite3_rest autoware_ar_tag_based_localizer
  
- 激活指令: 
  - 当我说时-开始发版工作流：请发版autoware:<name>/<YYMMDD>(如test/20260424,release/20260424)时，release note:1. 解决变道取消导致的轨迹突变；2. 优化尾部轨迹稳定性；3. 解决因为地图错误导致的规划崩溃；4. 解决因为地图高程过高导致轨迹出现在地下问题；5. 优化重规划功能；6. jpeg输出frame_id与camera_info统一；开始OTA发版工作流
  - release note: 是这个版本更新的内容，可以为空，格式是1.xxx 2.xxx 3.xxx 4.xxx
  - test/<YYMMDD>: 代表自动驾驶园区版本发版
  - release/<YYMMDD>: 代表自动驾驶公开道路版本

- 工作流程
  - step1: 创建ota配置文件制作并上传
    - step1-1: 使用`generate_ota_config.py`创建ota配置, 脚本会返回创建的路径
    - step1-2: 使用PixRover-Ota cfg_push  <配置文件路径>
    - step1-3: PixRover-Ota cfg_info查看配置文件是否上传成功
  - step2: cd进入对应的自动驾驶工作空间
  - step3: git pull
  - step4: git checkout <name>/<YYMMDD>
  - step5: git branch 确定分支是否成功切换为<name>/<YYMMDD>
  - step6: vcs import src < autoware.repos --recursive
  - step7: vcs import src < robobus.repos --recursive
  - step8: vcs import src < hmi.repos --recursive
  - step9: vcs pull src 
  - step10: 在自动驾驶工作空间下使用编译指令开始编译

- 注意事项: 
  - 需要通过分析所有的步骤执行，确保成功后在继续下一步
  - 步骤执行产生的日志需要保存到文件中方便查看
  - 如果某步骤执行出错,先提出解决方案，我同意在修改并解决


